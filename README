1. Uso el comando Mkdir para entrar dentro de la carpeta practica_evaluable y uso [git init]
2. Uso el comando [echo "Texto introducido" > documento1.txt] y lo vuelvo a realizar cambiando 1 por 2. Luego uso el comando [git add *.txt] porque en este caso tengo que guardar los cambios de todos los archivos txt del repositorio. Por ultimo [git commit -m "mensage"]
3. Uso el comando [echo "Texto añadido" >> documento1.txt] y lo uso tantas veces como lineas quiera y vuelvo a usar [git add documento1.txt]. Por ultimo [git commit -m "mensage"]
4. Uso el comando [echo "Texto añadido" >> documento2.txt] y lo uso tantas veces como lineas quiera y vuelvo a usar [git add documento2.txt]. Por ultimo [git commit -m "mensage"] 
5. Uso el comando [git reset --hard HEAD^]
6. Uso el comando [echo "Texto introducido" > documento3.txt] y el comando [echo "Texto añadido" >> documento3.txt] y vuelvo a usar [git add documento3.txt]. Por ultimo [git commit -m "mensage"]
7. Uso el comando [git reset --hard HEAD^]
8. Buscando el enlace en github uso el comando [git remote add origin https://github.com/Throne/Practica_evaluable.git]
9. Uso el comando [git branch mirama] y [echo "Texto introducido" > mirama1.txt] [echo "Texto introducido" > mirama2.txt]. Despues [echo "Texto añadido" >> mirama1.txt] [echo "Texto añadido" >> mirama2.txt]. Por ultimo [git commit -m "mensage"]
10. Cambio a la rama master usando [git switch master], luego fusiono las ramas con [git merge mirama], para borrar la rama uso [git branch -d mirama]
11. Para sincronizar uso el comando [git push origin master]
12. Para crear la etiqueta uso [git tag V1] y la sincronizo con [git push --tags]
13. Clono el repositorio con [Git clone https://github.com/Throne-998/Practica_evaluable Practica_evaluable_2]
14. Uso [git switch -c mirama] y [echo "Texto añadido" >> documento1.txt] luego uso [git commit "mensage"] y [git push origin master]
15. Uso [echo "Texto añadido" >> documento2.txt] luego uso [git commit "mensage"] y [git push origin master]
16. Cambio a la rama master usando [git switch master], luego fusiono las ramas con [git merge mirama], para borrar la rama uso [git branch -d mirama]
17. Uso [echo "Texto añadido" >> documento2.txt] luego [git add documento 2.txt] y [git commit]
18. Uso [git switch -c mirama] y [echo "Texto añadido" >> documento2.txt] luego uso [git commit "mensage"]
19. Uso [git merge mirama] y para resolver el conflico uso [:q!]
20. Uso [git push origin master]
